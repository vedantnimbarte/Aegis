# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0

# --- Database ---
sqlalchemy==2.0.36
psycopg[binary]==3.2.3
alembic==1.14.0

# --- Settings / validation ---
pydantic==2.10.4
pydantic-settings==2.7.0
email-validator==2.2.0  # EmailStr validation for email/password auth

# --- Background jobs ---
celery==5.4.0
redis==5.2.1

# --- Auth / crypto ---
python-jose[cryptography]==3.3.0
bcrypt==4.2.1
cryptography==44.0.0

# --- Integrations ---
httpx==0.28.1          # GitHub / OAuth calls
stripe==11.4.1
docker==7.1.0          # Docker Engine API for Strix containers

# --- Reporting ---
fpdf2==2.7.9           # pure-Python PDF export of scan reports

# --- Rate limiting ---
slowapi==0.1.9         # Redis-backed limits on abuse-prone auth endpoints

# --- Observability ---
prometheus-fastapi-instrumentator==7.0.0  # exposes /metrics for Prometheus

# --- Misc ---
python-multipart==0.0.20
